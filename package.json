{
  "name": "telegram-bot-vercel",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vercel dev",
    "deploy": "npx vercel --prod",
    "set-webhook": "node scripts/set-webhook.mjs"
  },
  "dependencies": {
    "grammy": "^1.34.1"
  },
  "devDependencies": {
    "vercel": "^41.0.0"
  }
}
